% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricecalculation.R
\name{priceCalculation}
\alias{priceCalculation}
\title{Calculate Required Sample Size and Total Cost for 16S and WGS to Achieve Target Power}
\usage{
priceCalculation(
  target_power = 0.8,
  lower_bound = 50,
  upper_bound = 500,
  step = 5,
  cost16s = 100,
  costWGS = 500,
  nSim = 100,
  alpha = 0.05,
  nOTU = 500,
  diff.otu.pct = 0.1,
  diff.otu.direct = "balanced",
  diff.otu.mode = "abundant",
  user_specified_otu = NULL,
  covariate.type = "binary",
  grp.ratio = 1,
  covariate.eff.mean = 1,
  covariate.eff.sd = 0,
  confounder.type = "none",
  conf.cov.cor = 0.6,
  conf.diff.otu.pct = 0,
  conf.nondiff.otu.pct = 0.1,
  confounder.eff.mean = 0,
  confounder.eff.sd = 0,
  error.sd = 0,
  depth.mu = 10000,
  depth.theta = 5,
  depth.conf.factor = 0,
  cont.conf = NULL,
  epsilon = NULL
)
}
\arguments{
\item{target_power}{Numeric. The desired statistical power (e.g., 0.8). Default is 0.8.}

\item{lower_bound}{Numeric. The minimum sample size to consider. Default is 50.}

\item{upper_bound}{Numeric. The maximum sample size to consider. Default is 500.}

\item{step}{Numeric. The increment between sample sizes. Default is 5.}

\item{cost16s}{Numeric. The estimated cost per sample for 16S sequencing. Default is 100.}

\item{costWGS}{Numeric. The estimated cost per sample for WGS sequencing. Default is 500.}

\item{nSim}{Numeric. Number of simulations to run per sample size. Default is 100.}

\item{alpha}{Numeric. Significance level for power calculation. Default is 0.05.}

\item{nOTU}{Numeric. Number of OTUs to simulate. Default is 500.}

\item{diff.otu.pct}{Numeric. Proportion of OTUs that are differentially abundant. Default is 0.1.}

\item{diff.otu.direct}{Character. Direction of differential abundance ("balanced" by default).}

\item{diff.otu.mode}{Character. Mode of differential abundance ("abundant" by default).}

\item{user_specified_otu}{Optional. User-specified OTU data; default is NULL.}

\item{covariate.type}{Character. Type of covariate ("binary" by default).}

\item{grp.ratio}{Numeric. Group ratio for the covariate. Default is 1.}

\item{covariate.eff.mean}{Numeric. Mean effect of the covariate. Default is 1.}

\item{covariate.eff.sd}{Numeric. Standard deviation of the covariate effect. Default is 0.}

\item{confounder.type}{Character. Type of confounder ("none" by default).}

\item{conf.cov.cor}{Numeric. Correlation between confounders and covariates. Default is 0.6.}

\item{conf.diff.otu.pct}{Numeric. Proportion of OTUs differentially affected by confounders. Default is 0.}

\item{conf.nondiff.otu.pct}{Numeric. Proportion of non-differential OTUs affected by confounders. Default is 0.1.}

\item{confounder.eff.mean}{Numeric. Mean effect of the confounder. Default is 0.}

\item{confounder.eff.sd}{Numeric. Standard deviation of the confounder effect. Default is 0.}

\item{error.sd}{Numeric. Standard deviation of error. Default is 0.}

\item{depth.mu}{Numeric. Mean sequencing depth. Default is 10000.}

\item{depth.theta}{Numeric. Dispersion parameter for sequencing depth. Default is 5.}

\item{depth.conf.factor}{Numeric. Depth configuration factor. Default is 0.}

\item{cont.conf}{Numeric vector. Continuous confounder values; if NULL, generated randomly. Default is NULL.}

\item{epsilon}{Numeric vector. Error vector; if NULL, generated randomly. Default is NULL.}
}
\value{
A list containing:
\describe{
  \item{required_sample_size_16s}{Minimum sample size required to achieve the target power for 16S data.}
  \item{total_cost_16s}{Total estimated cost for 16S data at the required sample size.}
  \item{required_sample_size_wgs}{Minimum sample size required to achieve the target power for WGS data.}
  \item{total_cost_wgs}{Total estimated cost for WGS data at the required sample size.}
}
}
\description{
This function runs power calculations using the simulation function \code{sampleCalculationRange} for both
16S (amplicon) and whole-genome shotgun (WGS) sequencing data. It estimates the minimum sample size required
to achieve a specified target power and calculates the total cost based on per-sample cost estimates for each method.
}
\examples{
\dontrun{
price_estimates <- priceCalculation(target_power = 0.8,
                                    lower_bound = 50,
                                    upper_bound = 500,
                                    step = 5,
                                    cost16s = 100,
                                    costWGS = 500,
                                    nSim = 100,
                                    alpha = 0.05,
                                    nOTU = 500,
                                    diff.otu.pct = 0.1,
                                    diff.otu.direct = "balanced",
                                    diff.otu.mode = "abundant",
                                    user_specified_otu = NULL,
                                    covariate.type = "binary",
                                    grp.ratio = 1,
                                    covariate.eff.mean = 1,
                                    covariate.eff.sd = 0,
                                    confounder.type = "none",
                                    conf.cov.cor = 0.6,
                                    conf.diff.otu.pct = 0,
                                    conf.nondiff.otu.pct = 0.1,
                                    confounder.eff.mean = 0,
                                    confounder.eff.sd = 0,
                                    error.sd = 0,
                                    depth.mu = 10000,
                                    depth.theta = 5,
                                    depth.conf.factor = 0,
                                    cont.conf = NULL,
                                    epsilon = NULL)
print(price_estimates)
}

}
